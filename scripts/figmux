#!/usr/bin/env bash
set -euo pipefail

# Some shells (e.g. VS Code integrated terminals) export this globally and force Electron into Node mode.
unset ELECTRON_RUN_AS_NODE

exec /app/electron/electron --no-sandbox /app/figmux/src/main.js "$@"
